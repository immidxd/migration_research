import React from "react";

// Placeholder for the period brush. Will be a d3-brush over a scale spanning
// 1800-1940, with named period overlays (waves, interwar, etc.) drawn on top.
const Timeline: React.FC = () => (
  <div className="h-full flex items-center justify-center text-white/40 text-sm">
    Timeline (period brush) — to be implemented
  </div>
);

export default Timeline;
